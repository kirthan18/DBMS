SELECT FOOD_DES.Long_Desc, COUNT(*) FROM FOOD_DES, NUT_DATA
WHERE
FOOD_DES.NDB_No = (SELECT NUT_DATA.NDB_No FROM NUT_DATA
                    GROUP BY NUT_DATA.NDB_No
                    ORDER BY COUNT(*) DESC LIMIT 1
                   )
GROUP BY NUT_DATA.NDB_No
ORDER BY COUNT(*) DESC LIMIT 1;
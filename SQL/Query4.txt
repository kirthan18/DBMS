TODO - Check if you have to print FOOD_DES.Long_Desc

SELECT FOOD_DES.Long_Desc, FOOD_DES.Shrt_Desc FROM FOOD_DES WHERE FOOD_DES.NDB_No = (SELECT NUT_DATA.NDB_No FROM NUT_DATA GROUP BY NUT_DATA.NDB_No ORDER BY COUNT(*) DESC LIMIT 1);